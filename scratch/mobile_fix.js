const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else if (file.endsWith('.tsx')) { results.push(file); } }); return results; } const files = walk('./src'); files.forEach(file => { let content = fs.readFileSync(file, 'utf8'); let original = content; content = content.replace(/className="([^"]*)\bpy-32\b([^"]*)"/g, 'className="-16 lg:py-32"'); content = content.replace(/className="([^"]*)\bpy-24\b([^"]*)"/g, 'className="-16 lg:py-24"'); content = content.replace(/className="([^"]*)\bpy-20\b([^"]*)"/g, 'className="-12 lg:py-20"'); content = content.replace(/className="([^"]*)\bgap-16\b([^"]*)"/g, 'className="-10 lg:gap-16"'); content = content.replace(/className="([^"]*)\bgap-12\b([^"]*)"/g, 'className="-8 lg:gap-12"'); if (content !== original) { fs.writeFileSync(file, content, 'utf8'); console.log('Updated ' + file); } });
